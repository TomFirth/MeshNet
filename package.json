{
  "name": "meshnet",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "npm test --workspaces",
    "build": "npm run build --workspaces",
    "start:relay": "npm start --workspace=@meshnet/relay",
    "start:simulator": "npm start --workspace=@meshnet/simulator"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "ts-node": "^10.9.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.11",
    "rimraf": "^5.0.10"
  }
}
